name: Release Plugin

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Plugin version (e.g. 1.0.0.0)"
        required: true
        default: "1.0.0.0"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Determine version
        id: ver
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            V="${GITHUB_REF_NAME#v}"
          else
            V="${{ github.event.inputs.version }}"
          fi
          # Strip any accidental leading/trailing whitespace (e.g. from manual dispatch input).
          V="${V// /}"
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "tag=v$V" >> "$GITHUB_OUTPUT"

      - name: Build
        run: dotnet build Jellyfin.Plugin.PrivateLibraries/Jellyfin.Plugin.PrivateLibraries.csproj -c Release

      - name: Package
        id: pkg
        run: |
          set -euo pipefail
          V="${{ steps.ver.outputs.version }}"
          mkdir -p dist staging

          cp Jellyfin.Plugin.PrivateLibraries/bin/Release/net10.0/Jellyfin.Plugin.PrivateLibraries.dll staging/
          ZIP="private-libraries_${V}_jf12.zip"
          (cd staging && zip -r "../dist/$ZIP" .)
          echo "zip=$ZIP" >> "$GITHUB_OUTPUT"
          echo "checksum=$(md5sum "dist/$ZIP" | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: Private Libraries ${{ steps.ver.outputs.version }}
          files: |
            dist/${{ steps.pkg.outputs.zip }}
          fail_on_unmatched_files: true

      - name: Update manifest
        env:
          VERSION: ${{ steps.ver.outputs.version }}
          REPO: ${{ github.repository }}
          TAG: ${{ steps.ver.outputs.tag }}
          ZIP: ${{ steps.pkg.outputs.zip }}
          CHECKSUM: ${{ steps.pkg.outputs.checksum }}
        run: |
          python3 - <<'PY'
          import json, os, datetime

          version = os.environ['VERSION']
          repo = os.environ['REPO']
          tag = os.environ['TAG']
          zipname = os.environ['ZIP']
          checksum = os.environ['CHECKSUM']
          ts = datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')

          path = 'manifest.json'
          with open(path) as f:
              data = json.load(f)
          plugin = data[0]
          entry = {
              "version": version,
              "changelog": f"Release {version}",
              "targetAbi": "12.0.0.0",
              "sourceUrl": f"https://github.com/{repo}/releases/download/{tag}/{zipname}",
              "checksum": checksum,
              "timestamp": ts,
          }
          vers = [v for v in plugin.get('versions', []) if v.get('version') != version]
          vers.insert(0, entry)
          plugin['versions'] = vers
          with open(path, 'w') as f:
              json.dump(data, f, indent=4)
              f.write('\n')
          print(f"{path}: added {version}")
          PY

      - name: Commit manifest to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git commit -m "Update plugin manifest for ${{ steps.ver.outputs.version }}" || { echo "No manifest changes"; exit 0; }
          git push origin HEAD:main
